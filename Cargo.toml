[package]
name = "buffetcar"
version = "0.1.0"
edition = "2021"

[features]
nexd-contract = []

[dependencies]

[[test]]
name = "nexd_contract"
path = "tests/nexd_contract.rs"
required-features = ["nexd-contract"]
